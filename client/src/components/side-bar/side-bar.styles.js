import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {},
  drawer: {
    width: "145px",
  },
  link: {
    padding: "15px",
    color: "#3F51B5",
    fontWeight: "500",
    fontSize: "large",
    textDecoration: "none",
  },
}));

export default useStyles;
